[workspace]
resolver = "2"
members = [
  "voting-machine",
  "voting-machine/core",
]

# Always optimize; otherwise tests take excessively long.
[profile.dev]
opt-level = 3

[profile.release]
debug = 1
lto = true
